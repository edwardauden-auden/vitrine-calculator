Flask>=3.0
# Pinned exactly, not >=: Playwright's Python package version must match
# the browser binaries baked into the Docker base image (see Dockerfile).
# An open-ended version here let pip install a newer client than the
# image's browsers, which broke every single request with
# "BrowserType.launch: Executable doesn't exist..." — match this to
# whatever version tag the Dockerfile's FROM line uses.
playwright==1.56.0
